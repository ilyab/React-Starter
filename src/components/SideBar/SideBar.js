import React from 'react';

import './SideBar.css';
import SideBarItems from './Items/Items';

const SideBar = (props) => {
    return (
        <div className='SideBar'>
            
        </div>
    );
}

export default SideBar;